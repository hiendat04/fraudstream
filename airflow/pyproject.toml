[project]
name = "fraudstream-airflow-dev"
version = "0.1.0"
description = "Local editor-only environment for Airflow DAG development (IDE support, not a runtime)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "apache-airflow==3.3.0",
    "pyspark==4.1.2",
    "feast[spark,postgres,redis]==0.66.0",
]

[tool.uv]
package = false
