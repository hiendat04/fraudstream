[project]
name = "financial-fraud-detection"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fraudstream-generate-offline = "fraudstream.generators.offline_transactions:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
