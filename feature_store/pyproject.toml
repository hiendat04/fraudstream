[project]
name = "fraudstream-feature-store"
version = "0.1.0"
description = "Isolated Feast feature-store runtime for FraudStream"
requires-python = ">=3.12,<3.13"
dependencies = [
    "feast[spark,postgres,redis,grpcio]==0.66.0",
    "pyspark==4.1.2",
    "confluent-kafka>=2.6,<3",
    "grpcio>=1.60,<2",
]

[tool.uv]
package = false
