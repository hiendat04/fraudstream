[project]
name = "fraudstream-flink-runtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "apache-flink==2.2.1",
]

[tool.uv]
package = false

[tool.uv.extra-build-dependencies]
apache-beam = ["setuptools<72"]
